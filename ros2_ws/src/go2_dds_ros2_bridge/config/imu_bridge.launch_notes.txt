First-stage IMU bridge notes:
- Publishes sensor_msgs/Imu from DDS rt/lowstate.
- Reconstructs header.stamp from the known 1 ms LowState cadence and locally re-anchors when timing error grows too large.
- Publishes each reconstructed IMU sample directly instead of rate-limiting through a lower-frequency flush timer.
- Still needs runtime cadence validation and LiDAR alignment checks before FAST-LIO2 becomes the primary odometry path.